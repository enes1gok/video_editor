export default {
    plugins: {
        '@tailwindcss/postcss': {},
        autoprefixer: {}, // Optional, but usually handled by v4? keeping just in case or removing if redundant.
        // Actually v4 docs say "You don't need autoprefixer anymore".
        // I will remove it to be clean.
    },
}
